# credentials — never commit
.env
client_secret*.json
service_account*.json
myproject*.json
session.json

# instagram cookies — never commit
*_cookies.txt

# scrape output files (committed by workflow only)
reels_*.json

# python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
